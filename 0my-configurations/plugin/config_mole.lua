require'mole'.setup {
    keys = {
        annotate = '<leader>pa',        -- visual mode
        start_session = '<leader>ps',   -- normal mode
        stop_session = '<leader>pq',    -- normal mode
        resume_session = '<leader>pr',  -- normal mode
        toggle_window = '<leader>pw',   -- normal mode
        --jump_to_location = { "<CR>", "gd" }, -- in side panel
        --next_annotation = "]a",              -- in side panel
        --prev_annotation = "[a",              -- in side panel
    },
}
